import {Text} from '@rneui/themed';
import {SafeAreaView} from 'react-native-safe-area-context';

export default function AdminNotificationScreen(): JSX.Element {
  return (
    <SafeAreaView>
      <Text>AdminNotificationScreen</Text>
    </SafeAreaView>
  );
}
