import {StyleSheet, View} from 'react-native';

export default function CreateShopAccountScreen(): JSX.Element {
  //shop address
  // phone
  // name
  // image uri
  return <View style={styles.container}></View>;
}

const styles = StyleSheet.create({
  container: {},
});
