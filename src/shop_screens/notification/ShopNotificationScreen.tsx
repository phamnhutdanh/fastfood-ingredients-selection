import {Text} from '@rneui/themed';
import {View} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

export default function ShopNotificationScreen(): JSX.Element {
  return (
    <SafeAreaView>
      <Text>ShopNotificationScreen</Text>
    </SafeAreaView>
  );
}
