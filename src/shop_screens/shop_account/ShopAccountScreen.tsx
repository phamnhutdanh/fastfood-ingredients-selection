import {Text} from '@rneui/themed';
import {View} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

export default function ShopAccountScreen(): JSX.Element {
  return (
    <SafeAreaView>
      <Text>ShopAccountScreen</Text>
    </SafeAreaView>
  );
}
